{
    "version": 2,
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/node",
            "config": {
                "includeFiles": ["src/db.json"]
            }
        }
    ],
    "routes": [{"src": "/(.*)", "dest": "/api/*"}],
    "dest": "server.js"
}